# 8.104 Breadth-first traversal
Video•
. Duration: 8 minutes
8 min - Summary

URL: https://www.coursera.org/learn/uol-algorithms-and-data-structures-2/lecture/ejoFz/8-104-breadth-first-traversal

Here is a summary of the text in 25 sentences, preserving all key information, formulae, links, and technical details:

1. The lecture will focus on the implementation of breadth-first traversal of a tree.
2. Breadth-first traversal is more complicated to implement than depth-first traversal because it does not take advantage of the vertical recursive structure of the tree.
3. In contrast to depth-first traversal, which visits nodes in a specific order (e.g., left-to-right), breadth-first traversal visits all nodes at a given level before moving on to the next level.
4. The lecture assumes a binary tree as an example of a tree that can be traversed using both methods.
5. To traverse a tree using breadth-first traversal, we need to visit each node in a level before moving on to the next level.
6. This means that we need to keep track of all nodes at a given level and their children.
7. The lecture provides an example implementation of breadth-first traversal using a queue data structure.
8. In this implementation, we use a queue to store nodes at each level.
9. We start by adding the root node to the queue.
10. Then, we enter a loop that continues until the queue is empty.
11. Inside the loop, we remove the first node from the queue and visit it.
12. After visiting the node, we add its children to the end of the queue.
13. This process repeats until the queue is empty, at which point all nodes have been visited.
14. The lecture provides an example tree that can be traversed using breadth-first traversal.
15. The resulting output should match the sequence a, b, c, d, e, f, g, h.
16. The lecture emphasizes that breadth-first traversal may be easier to follow for Western humans who are used to reading from top-to-bottom and left-to-right.
17. However, in terms of programming effort, depth-first traversal is generally easier to implement than breadth-first traversal.
18. Therefore, the choice between these two methods depends on the specific problem being solved and which method makes it easier to solve the problem or check its correctness.
19. The lecture also notes that both traversal algorithms can be modified to work with trees that are not binary.
20. To do this, we would need to modify the data structure used to represent the tree and the algorithm for traversing it.
21. For example, a non-binary tree might use an array or linked list to store its nodes instead of a tree data structure.
22. In this case, we would need to modify the algorithm to handle the specific data structure being used.
23. The lecture emphasizes that understanding how to traverse trees is an important skill for any programmer.
24. By learning about both depth-first and breadth-first traversal, programmers can choose the best approach for their specific problem or project.
25. The lecture concludes by emphasizing the importance of practice and simulation in mastering these skills.

Note: I omitted the additional page content as it was not relevant to the main topic of the lecture, which was on breadth-first traversal.
# 8.102 Depth-first traversal
Video•
. Duration: 9 minutes
9 min

URL: https://www.coursera.org/learn/uol-algorithms-and-data-structures-2/lecture/qMItV/8-102-depth-first-traversal

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Hi. In this lecture, we will study the implementation of the three different ways of visiting all the nodes of a binary tree using depth-first traversal. In the previous lecture, I talked to you about three different ways of traversing a tree vertically; pre-order, in-order, and post-order. The prefix used in the names of the traversals refers to the order we use to visit the root node. Thus, if we use pre-order, the root node precedes everything. Then we visit the left sub-tree and the right sub-tree, following the same rule. The root of every sub-tree precedes everything. If we use in-order, the root is visited in the middle of the traversal. If we use post-order, the root node is visited at the end. If we have a quick look at the pseudocode of this three traversals, we can immediately notice two important things. First, in the pseudocode of the pre-order traversal, the function visit is the first one to be called. In in-order traversal, it's the second one to be called, and in the post-order traversal, it's the last one to be called. Second, these are all recursive functions. That is, the same function is called inside itself. Because trees have a vertical recursive structure, a tree is made of sub-trees which are in turn made of sub-trees, using recursive functions to implement many operations facilitates coding. Now, let's simulate the execution of each line inside the traversal function pre-order to understand what task they perform. The triangle on top of the tree signals the part of the tree being entered as input argument to the different recursive calls. This first line checks that the tree, denoted by the letter T, is not empty. The exclamation mark before the function is empty is used as a logical node operator. Thus, if the tree has at least one node, then we execute the instructions inside the if statement. That is, we continue with the recursion. If the tree is empty, the recursion is stopped. In this case, we continue with the recursion. The first instruction inside the if statement is visiting the root node of the tree. In this case, we will assume that visit means printing the content of the note on the screen. We will simulate this by writing the content of the note inside brackets in the upper right part of the screen. The next instruction is a recursive call using as input argument, the left sub-tree of the original tree. So we open a new pseudocode box and check if this sub-tree is empty. It's not. So we proceed to execute the instructions inside the if statement. We visit the root of this sub-tree by printing its content on the screen, and move to the next instruction. In this case, we make a recursive call again using as input argument the next left sub-tree. This sub-tree is not empty, so we proceed to execute the instructions inside the if statement. We print the content of the node on a screen and call, again, the recursive function using as input argument, the next left sub-tree. Because this sub-tree is empty, the next recursive call will return immediately without printing anything on a screen. So I will not show it here. Notice that by now, using a pre-order traversal, the root of the original tree and the roots of all sub-trees on the left side have already been printed. So this traversal visits first all the root nodes on the left of the tree. Now, we move to the right sub-tree, but this is also an empty tree, so I will not show here the execution of a recursive function, as it will return immediately. With this, this recursive call finishes, so it's eliminated from memory. Now we execute a recursive call with the right sub-tree. This sub-tree is not empty, so we execute the instructions inside the if statement. We print the content of the node and we make a recursive call using as input argument, the left sub-tree. However, this sub-tree is empty, so the recursive call will return immediately. I will not show that execution here. The same thing happens with the right sub-tree. Again, I will not show it, but this, the execution of this recursive call finishes and it's eliminated from memory. This recursive call has also finished and is eliminated from memory. Notice that, at this point, the root of the tree and all the nodes of the left sub-tree have been visited following the pre-order rule. Root first, then left sub-tree, then right sub-tree. If you apply the same rule to the right sub-tree, you should first print the root node, that is node C. , the nodes in the left sub-tree of C, which is empty, so there's nothing to print. Then, all the nodes in the right sub-tree of C, which is only f. You can simulate that part of the code on your own to check that the final information being printed on the screen is a, b, d, e, c, and f. If you look at the order of visit of the nodes, you can see that the root goes first, then all nodes on the left sub-tree, and then all the nodes on the right sub-tree. If you look at the order of the nodes of the left sub-tree, you will observe the same pattern again. Root first, left sub-tree next, and right sub-tree later, you will see the same structure on the right sub-tree. On a general overview of the different traversals, we can't see that by executing the pre-order traversal with this tree, the root precedes everything. Then we move to the nodes on the left sub-tree and then to the nodes on the right sub-tree. If you execute the in-order traversal, the root of the tree will appear in the middle, immediately after all nodes of the left sub-tree have been visited, and immediately before all nodes of the right sub-tree have been visited. If you look at the order used to visit the nodes in the left and right sub-trees, you will see the same structure again. Finally, if you execute the post-order traversal, the root node of the tree will be visited last. First, all nodes of the left sub-tree will be visited, then all nodes of the right sub-tree, and finally the root. As in the previous traversals, if you look at the order in which the nodes of every sub-tree are printed, you will identify the same structure. Left sub-tree first, right sub-tree next, and finally the root node. With this, we finish our study of depth-first traversal. In the next lecture, we will see how to implement breadth-first traversal. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1: Analysis of algorithms, part 1 ### Week 2 Topic 1: Analysis of algorithms, part 2 ### Week 3 Topic 2: Recursive algorithms, part 1 ### Week 4 Topic 2: Recursive algorithms, part 2 ### Week 5 Topic 3: Comparison sorting algorithms, part 1 ### Week 6 Topic 3: Comparison sorting algorithms, part 2 ### Week 7 Topic 4: Non comparison sorting algorithms, part 1 ### Week 8 Topic 4: Non comparison sorting algorithms, part 2 ### Week 9 Topic 5: Hashing, part 1 ### Week 10 Topic 5: Hashing, part 2 ### Week 11 Topic 6: Mid-term coursework time ### Week 12 Mid-term coursework time and submission ### Week 13 Topic 7: Linked lists, stacks and queues, part 1 ### Week 14 Topic 7: Linked lists, stacks and queues, part 2 ### Week 15 Topic 8: Trees, part 1 - 8.001 Trees: Introduction Video . Duration: 5 minutes 5 min - 8.002 Trees: Introduction Practice Assignment . Duration: 40 minutes 40 min - 8.003 Binary trees: Implementation Video . Duration: 4 minutes 4 min - 8.004 Binary trees: Implementation Practice Assignment . Duration: 30 minutes 30 min - 8.101Binary tree traversal: Introduction Video . Duration: 3 minutes 3 min - 8.102 Depth-first traversal Video . Duration: 9 minutes 9 min - 8.103 Depth-first traversal Practice Assignment . Duration: 30 minutes 30 min - 8.104 Breadth-first traversal Video . Duration: 8 minutes 8 min - 8.105 Breadth-first traversal Practice Assignment . Duration: 30 minutes 30 min ### Week 16 Topic 8: Trees, part 2 ### Week 17 Topic 9: Heaps, part 1 ### Week 18 Topic 9: Heaps, part 2 ### Week 19 Topic 10: Graphs, part 1 ### Week 20 Topic 10: Graphs, part 2 ### Week 21 Preparation to the exam ### Week 22 Exam week # 8.102 Depth-first traversal ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key
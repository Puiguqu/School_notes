# 3.202 Quicksort: Pseudocode
Video•
. Duration: 13 minutes
13 min

URL: https://www.coursera.org/learn/uol-algorithms-and-data-structures-2/lecture/qr49f/3-202-quicksort-pseudocode

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Hello in this lecture, we will review quicksort. Together with mergesort, quicksort is a comparison sorting algorithm that is very easy to code if you use recursion. We will start by understanding what part of the code is in charge of what task, and then simulate the execution of the algorithm with a specific small input. This is the pseudocode for the recursive implementation of quicksort. First let's analyze its structure as a recursive algorithm. A recursive algorithm can be recognized easily because inside its code you find one or more calls to the same algorithm. In this case quicksort calls itself twice. The first call must work on the section of the array that starts at position low, and finishes at position p-1. Whereas the second call works on the section of the array that starts at position p+1 and ends at position high. Notice that in this image the positions low, p, and high are just possible values. As the execution of the algorithm progresses, they might point to different positions in the array, although the relative order remains. Now a recursive call should also include at least a base case, that is a condition where the recursion stops. In this implementation the base case is implicit in the non-written else part of the if statement. That is if the condition low lower than high is met, then the recursion continues. But if this condition is not met the recursive function simply returns and recursion stops. This is the same as having a code with an explicit else part like this. However, as this extra code performs exactly the same function as simply ending the if and then finishing the function, it is not included and the base case remains implicit. Now let's understand what part of the code is in charge of what task. We have already seen that this this if statement is in charge of deciding whether the recursion is stops or continues. The first instruction inside the if statement, the function partition, is in charge of three tasks. One, selecting the number inside the array, the pivot. Two, moving all the numbers lower than the pivot to the left part of the array. Three, moving the pivot to its final position in the sorted array. That is in the position immediately to the right of the highest number lower than the pivot. In this example you can see that after executing the function partition, all numbers lower than the pivot can be found between positions low and p-1. And all numbers higher than the pivot can be found between positions p+1 and high. Thus the pivot is in its final position. For now we will only work with this idea of the function partition without dealing with the details of the code inside it. We will look at the details of that code later on. Finally this part of the code is in charge of processing the two halves of the array A. The first recursive call processes the half of the array to the left of the pivot. And the second recursive call processes the right half. Let's now simulate the execution of the algorithm with this specific small input, an array made up of five elements. As low and high are used to represent positions in the array, we will represent them by pointing to the corresponding positions. The first instruction checks whether the variable low has a value lower than high. In this case 0 is lower than 4, so we execute the instructions inside the if statement. Now, we execute the function partition. This function performs three steps. First the function partition selects one number in the array. In this implementation the function partition will always select the number in position high, which is 17. This number is called the pivot. the content of the array between positions low and high is rearranged in such a way that all numbers lower than the pivot 17 are in the left part of the array, and all numbers greater than the pivot are in the right part of the array. The pivot is in between. You can see here that numbers 12 and 3, which are lower than 17, are in the left part of the array. While numbers 23 and 31 are in the right part of the array, and the pivot is located in its final position. Finally, the position of the pivot is returned to the quicksort function. As variable p represents a position in the array, it will be represented in the same way as variables low and high. In this case number 2 is returned. We now make the first recursive call. To represent this, we open a new function quicksort with the new input arguments. We'll start by checking whether low is lower than high. This is true, so we proceeded to execute the instructions inside the if statement. The function partition must be executed. Remember that this function performs three tasks. First the element in position high is chosen as the pivot, in this case number three. Secondly, the contents of the part of the array between positions low and high, are rearranged so all numbers lower than the pivot are located to its left, and all numbers greater than the pivot are located to it's right. Finally the position of the pivot is returned and stored in variable p. Now a recursive call to quicksort is made again. To be able to visualize this second recursive call, the boxes with the pseudocode will now be compressed. The information on the screen will be exactly the same, but in a smaller size so as to fit in the next recursive call. So here we have exactly the same information, but in a reduced format. Now, we proceed to open another box to represent the execution of the second recursive call. This new recursive call receives as input arguments the array A and the values of low which is 0, and p-1, which is -1. We first check the condition to continue with the execution of the recursive call. In this case the condition is false as the variable low is not lower than the variable high. So the execution of the if statement ends as well as the execution of this recursive call. This function is then eliminated from memory and we go back to the previous recursive call. We proceed now to the next recursive call here, with the arguments A, p+1 which is 1, and high, which is also 1. So we open a new pseudocode box with the function quicksort. We check the condition of the if, but low is not a lower than high so the recursion ends as the if statement is not executed. And the execution of the recursive call finishes. This function is then eliminated from memory and we go back to the previous recursive call. In this recursive call there is nothing else to do. We finish the execution of the if statement, and the execution of the function as well. The function is eliminated from memory, and we go back to the previous function. Notice that by now, after executing the recursion chain initiated by the first recursive call of quicksort, the left half of the array is already sorted. Now we have to process the right half of it. This processing starts by calling quicksort with input arguments that make the function to process the right half of the array. So we open a new function with input argument low equal to 3, and high equal to 4. We start by checking the condition of the if statement. Is low lower than high? Yes it is, so we proceed to execute the instructions inside the if statement. The function partition must be executed. Remember that this function performs three tasks. First the element position high is chosen as the pivot. In this case it's number 31. Second, the contents of the part of the array between positions low and high are rearranged so all numbers lower than the pivot are located to its left, and all numbers greater than the pivot are located to it's right. In this case, number 23 is already to the left of number 31. So no changes are made to the array content. Finally the position of the pivot is returned and stored in variable p. After finishing the execution of the function partition, a recursive call to quicksort is made again. As usual, we represent this by opening a new box with the pseudocode of quicksort. We'll start by checking the condition of the if statement. Is low lower than high? No it's not, so we skip the execution of the instructions inside the if statement, and the execution of the recursive call finishes. The function is deleted from memory and we go back to the previous recursive call. We move to the next instruction, that is another recursive call. So we open a new box with the code of quicksort receiving as the input arguments the array A and the values 5 and 4 for low and high. We'll start by checking the condition of the if statement. Is low lower than high? No it's not, so we skip the execution of the instructions inside the if statement, and the execution of the recursive call finishes. The function is deleted from memory, and we go back to the previous recursive call. The previous recursive call also ends and it gets deleted from memory. We go back to the original execution of quicksort with both halves of the array sorted and the pivot between them. There is no extra instruction to be executed. The function ends and the array is sorted. Now that you have a better understanding of how quicksort works, it's your turn to work on the following tasks. Quicksort is a comparison sort. The most important comparisons, the ones that help to sort the content of the array, are carried out by the partition function. But we did not look at the pseudocode of this function. Your task is to write the pseudocode of the function partition. To do so, look at the input arguments, and return value of this function. And remember that the three tasks performed by it are selecting the number in position high as the pivot, moving the numbers lower than the pivot to the left of the array, and the pivot immediately after them, returning the position of the pivot. You can check that your pseudocode is correct by translating it into real code in C++ and checking that it works for different cases. Your second task is calculating the worst and best case time complexity of quicksort. Can you apply the master theorem to find this out? ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1: Analysis of algorithms, part 1 ### Week 2 Topic 1: Analysis of algorithms, part 2 ### Week 3 Topic 2: Recursive algorithms, part 1 ### Week 4 Topic 2: Recursive algorithms, part 2 ### Week 5 Topic 3: Comparison sorting algorithms, part 1 ### Week 6 Topic 3: Comparison sorting algorithms, part 2 - 3.201 Quicksort: Demonstration Video . Duration: 2 minutes 2 min - 3.202 Quicksort: Pseudocode Video . Duration: 13 minutes 13 min - 3.203 Quicksort Practice Assignment . Duration: 30 minutes 30 min - 3.204 Quicksort Reading . Duration: 1 hour 1h - 3.301 Mergesort: Demonstration Video . Duration: 3 minutes 3 min - 3.302 Mergesort: Pseudocode Video . Duration: 7 minutes 7 min - 3.303 Mergesort: Step-by-step execution Video . Duration: 13 minutes 13 min - 3.304 Mergesort Practice Assignment . Duration: 30 minutes 30 min - 3.305 Mergesort Reading . Duration: 1 hour 30 minutes 1h 30m - Week 6 self-evaluation and reflection Reading . Duration: 15 minutes 15 min - 3.401 End of Topic 3 Video . Duration: 1 minute 1 min - 3.402 Mind map Practice Peer-graded Assignment . Duration: 1 hour 30 minutes 1h 30m - 3.403 Topic 3 summative assessment [001] Graded Assignment . Duration: 30 minutes 30 min ### Week 7 Topic 4: Non comparison sorting algorithms, part 1 ### Week 8 Topic 4: Non comparison sorting algorithms, part 2 ### Week 9 Topic 5: Hashing, part 1 ### Week 10 Topic 5: Hashing, part 2 ### Week 11 Topic 6: Mid-term coursework time ### Week 12 Mid-term coursework time and submission ### Week 13 Topic 7: Linked lists, stacks and queues, part 1 ### Week 14 Topic 7: Linked lists, stacks and queues, part 2 ### Week 15 Topic 8: Trees, part 1 ### Week 16 Topic 8: Trees, part 2 ### Week 17 Topic 9: Heaps, part 1 ### Week 18 Topic 9: Heaps, part 2 ### Week 19 Topic 10: Graphs, part 1 ### Week 20 Topic 10: Graphs, part 2 ### Week 21 Preparation to the exam ### Week 22 Exam week # 3.202 Quicksort: Pseudocode ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key
# 5.003 Hash tables
Video•
. Duration: 17 minutes
17 min

URL: https://www.coursera.org/learn/uol-algorithms-and-data-structures-2/lecture/AyFES/5-003-hash-tables

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Hello, in this lecture, you will learn about one of the most common uses of hashing, searching for a specific element in an array. The problem of searching is a classical problem in computer science that arises in many situations. For example, each time you log into a system, there's an algorithm in charge of searching for your username. If the search takes too long, you are very likely to give up entering the system. Successful systems really want you to enter quickly. In this lecture, I'm going to show you three ways of solving the problem of searching before moving on to how hashing can be used to solve it. In this way, you will have an overview of different approaches for searching. Most probably, you already know some of them. Let's start by defining the problem of searching in the context of this lecture. The input data will be an array of numbers and a number to search for. The algorithm must give us a result, the Boolean value true if the number is in the array, and the Boolean value false if the number is not in the array. There is a variant to this problem where the algorithm must give as a result the position of the number. If the number is not the array, the result is minus 1. In this topic, we will focus on the algorithm returning Boolean values, true if x is in the array and false if x is not in the array. Now that we know what the problem to solve is, let's review four algorithmic solutions to it. Let's start with solution one, linear search. Linear search simply consists of checking position by position in the array until you either find the number, in which case the algorithm returns the Boolean value true. Or you finish checking each position of the array and the number was not found in any position, in which case the algorithm returns the Boolean value false. This is the pseudocode of linear search. We use this for loop to check the content of each element in the array. If the element is found in one of the positions of the array, the Boolean value true is returned and the algorithm finishes its execution. If the for loop is finished without having found the element, the Boolean value false is returned. Now, let's have a look at the time complexity of linear search. Let's start with the worst case that is, the case where the maximum number of operations is performed. This happens when the number is not found in the array. In the worst case, all iterations of the for loop are executed, therefore this instruction performs a number of operations linear with n, the size of the array. The verification of this condition is carried out and times, each time a constant number of time units is taken. In the worst case, this return is never executed and finally, this return instruction takes constant time. So in the worst case, linear search is theta n. In the best case, the number is found in the very first position of the array. In that case, the for loop only performs one iteration and that takes constant time. The verification of the condition of the if statement also takes constant time and the instruction returning with the Boolean value true also takes a constant time. Thus, in the best case, linear search is theta 1. In terms of memory, linear search only creates a simple variable, the variable i. Therefore, the space complexity of linear search is theta 1. Now, let's move onto solution two, binary search. Binary search requires that the array is already sorted. In this slide, the value of the number inside each array position is represented in an abstract way by how much each box is filled with the teal color. In this case, we assume that the array is sorted from smallest to largest value, but it can also be the other way around. Binary search starts by checking the middle position of the array. If the element is found in that position, the algorithm returns true and its execution ends. If the element is not found in that position, the algorithm checks whether the number being searched for is lower than or greater than the element stored in the middle position. If the element being searched for has a lower value, for example, then we know it's not necessary to search the right half of the array. So, we discard that half of the array from further consideration and repeat the algorithm in the left half, selecting its middle point. If the element is found in the new middle point, the Boolean value true is returned and the algorithm ends, otherwise, a new verification is done. Is the number being searched for lower or greater than the element stored in the middle position? If for example, the element being searched for is greater than the element in the middle position, then we know it's not necessary to search the left half of the array. So, we discard that left half from further consideration and repeat the algorithm in the right half, selecting its middle point. If the element is found in this new middle point, the Boolean value true is returned and the algorithm ends. Otherwise, we know that the value is definitely not in the array. By discarding portions of the array, binary search makes a lower number of comparisons than linear search in the worst case. In this particular case, instead of doing seven comparisons, that is, checking every single array position, binary search will make only four comparisons in the worst case. On the left, you can see the iterative description of binary search using pseudocode, and on the right, the recursive one. Both check on the condition to either continue with the iteration or a stop the recursion and perform the same verifications to determine whether the number is in the array or not. Now, you can prove that no matter what the implementation, binary search time complexity is theta log n in the worst case and theta 1 in the best case. In terms of a space complexity, both implementations differ. The iterative version only creates a single new variable mid, therefore, it's a space complexity is theta 1. On the other hand, the recursive implementation creates a new variable mid every time a new recursive call is made. As the maximum number of recursive calls is log n, then the space complexity of the recursive implementation is theta log n. So, the complexity of the solutions to the problem of searching that we have looked at so far is summarized in this table. If you want a fast algorithm, you must go for binary search because it's worst case time complexity is theta log n, much better than the complexity of linear search. Also, because the difficulty of implementing the iterative version of binary search is not high, we would prefer the iterative implementation because it uses less memory space. Now, the question is, can we do better than theta log n when searching for a number? Yes, we can, and that leads us to solution three. Solution three is called direct addressing, and what this does is use the index of an array to represent a number. For example, let's consider the following original array of numbers. A new array is created where each index represents a number. This new array is called bit vector because it will only store ones and zeros. If the content of the bit vector in position i is 1, then it means that number i is present in the original array. If the content of the array in position i is 0, that is an indication that number i is not present in the array. In this example, the presence of number 8 in the original array is represented by storing a number 1 in position 8 of the bit vector. Instead, all numbers not present in the original array have zeros stored in their corresponding position. Now, searching for a number using the bit vector is really fast. This is the pseudocode of it once you have created the bit vector. You simply access the content of the bit vector in position x. As a result, this search in solution takes constant time to be executed, that is, the time complexity is theta 1. However, this fantastic running time comes at the expense of memory. You need to create a bit vector with a size equal to the maximum value stored in the original array plus 1. So, the higher the maximum number in the original array, the more extra memory you need to use direct addressing. For example, in the programming language C, the maximum unsigned integer representable with 32 bits would require a bit vector with four million boxes. Can we get a time complexity as good as direct addressing without using so much memory? Well under certain conditions we can, and that leads us finally to solution four, which is based on the idea of hashing. Similar to direct addressing, we will also use an array to store the numbers. This array is called the hash table. When we haven't restored any number yet in the hash table, the table is filled with minus one, or any other number that you know is out of the range of the numbers you will store there. Now, unlike direct addressing, the hash table has a much lower number of elements than the bit vector used indirect addressing. In this example, you can see that instead of having an array with 12 positions to store numbers from 0 to 11, we only have a hash table with seven positions. The hash table can be any size but if you know beforehand that you need to store seven numbers, you make a hash table with exactly seven numbers as in this example. Now, we cannot directly store number K in position K as done indirect addressing, because we do not have enough positions to do so. In this example, numbers 8 and 11 could not be represented in the hash table if we used direct addressing. So what we do now is transform each number into an index in the hash table using a hash function. In this example, we are going to use the hash function h(x) = x modulus 7. So, we apply this function to each number present in the array to determine the position at which they will be stored in the hash table. Let's start with number 8. We apply the hash function to number 8, 8 modulus 7 is equal to 1. So, number 8 is stored in position 1 in the hash table. Applying the same hash function, number 2 goes to position 2, number 11 goes to position 4, number 3 is stored in position 3. Number 5 goes to position 5, number 0 is a stored in position 0 and finally, number 6 is stored in position 6. Now that the numbers are stored in the hash table, searching for them is really fast here. Here, you can see the pseudocode to search for number x in hash table H. We simply apply whichever hash function we are using to the number we are searching. In doing so, we obtain the index where this number should be stored in the hash table. If the number is found there, we return true. Otherwise, we return false. Let's simulate the execution of this pseudocode with this hash table and the hash function h(x) = x modulus 7. Let's assume we are searching for number 11. The search algorithm first calculates the index where number 11 should be stored if it was in the trash table, that is, index 4. , we check the content of position 4, as position 4 has number 11 stored in it, the condition is true. And hence, the Boolean value true is returned signaling that the number has been found. Let's look at a second example with a number that is not present in the hash table, such as number 15. We apply the hash function to number 15, 15 modulus 7 is 1. So, the variable index takes the value 1. Now, we check whether number 15 is a stored in the position pointed out by the index. Now, number 15 is not stored in position 1, so we return false. You can't prove that this algorithm has a time complexity equal to theta 1, that is, the time taken to execute the search is constant and it does not depend on the size of the hash table, which is the best we can get. That makes hashing for searching a very good solution as you can see in this summary table. If you look at the two last rows, you will see that using direct addressing and a hash table have a time complexity of theta 1. However, in terms of memory usage, the use of a hash table is better because the number of elements in the hash table also called buckets is much lower than the number of elements of the bit vector used indirect addressing. So, we have seen that using hashing for searching is very efficient. However, we won't always have such a good situation as this. In this case, we knew the numbers that could be stored in the hash table beforehand. In knowing that, we could divide a hash function that was just perfect for this set of numbers. But what happens if you don't know in advance, how many and what numbers are going to be stored in your hash table? We will study that case, the problems that arise and how to solve them in the next lecture. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1: Analysis of algorithms, part 1 ### Week 2 Topic 1: Analysis of algorithms, part 2 ### Week 3 Topic 2: Recursive algorithms, part 1 ### Week 4 Topic 2: Recursive algorithms, part 2 ### Week 5 Topic 3: Comparison sorting algorithms, part 1 ### Week 6 Topic 3: Comparison sorting algorithms, part 2 ### Week 7 Topic 4: Non comparison sorting algorithms, part 1 ### Week 8 Topic 4: Non comparison sorting algorithms, part 2 ### Week 9 Topic 5: Hashing, part 1 - 5.001 What is hashing? Video . Duration: 7 minutes 7 min - 5.002 What is hashing? Practice Assignment . Duration: 30 minutes 30 min - 5.003 Hash tables Video . Duration: 17 minutes 17 min - 5.004 Hash tables Practice Assignment . Duration: 40 minutes 40 min - 5.101 Collisions in hash tables Video . Duration: 11 minutes 11 min - 5.102 Collisions in hash tables Practice Assignment . Duration: 40 minutes 40 min - 5.103 Hashing Reading . Duration: 2 hours 2h ### Week 10 Topic 5: Hashing, part 2 ### Week 11 Topic 6: Mid-term coursework time ### Week 12 Mid-term coursework time and submission ### Week 13 Topic 7: Linked lists, stacks and queues, part 1 ### Week 14 Topic 7: Linked lists, stacks and queues, part 2 ### Week 15 Topic 8: Trees, part 1 ### Week 16 Topic 8: Trees, part 2 ### Week 17 Topic 9: Heaps, part 1 ### Week 18 Topic 9: Heaps, part 2 ### Week 19 Topic 10: Graphs, part 1 ### Week 20 Topic 10: Graphs, part 2 ### Week 21 Preparation to the exam ### Week 22 Exam week # 5.003 Hash tables ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key
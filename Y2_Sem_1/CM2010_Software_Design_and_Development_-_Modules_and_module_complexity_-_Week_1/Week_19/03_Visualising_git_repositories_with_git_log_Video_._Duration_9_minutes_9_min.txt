# Visualising git repositories with git log
Video•
. Duration: 9 minutes
9 min - Summary

URL: https://www.coursera.org/learn/uol-cm2010-software-design-and-development/lecture/H73Au/visualising-git-repositories-with-git-log

Here is the transcript with added timestamps and a more detailed format:

**Visualizing Git Repositories with git log**

[Intro music plays]

Narrator: "Welcome to our tutorial on visualizing Git repositories using the `git log` command. In this video, we'll explore how to use `git log` to visualize the branching structure of a repository."

[0:00 - 0:30]
**A blank Repository**
```bash
$ git init
$ echo "This is a test commit" >> README.md
$ git add .
$ git commit -m "Initial commit"
```
Narrator: "Let's start with a blank repository. We'll create some commits to demonstrate how `git log` works."

[0:30 - 1:00]
**Creating a branch**
```bash
$ git branch feature/new-feature
$ git checkout feature/new-feature
$ echo "This is a new feature" >> README.md
$ git add .
$ git commit -m "Added new feature"
```
Narrator: "Now, let's create a new branch called `feature/new-feature` and make some commits on it."

[1:00 - 2:30]
**Visualizing the branching structure**
```bash
$ git log --all --oneline --graph
```
Narrator: "We'll use the `-g` flag to show the graphical representation of our commit history. This will give us a visual idea of how the branches are connected."

[2:30 - 3:00]
**Merging the branch**
```bash
$ git checkout master
$ git merge feature/new-feature
```
Narrator: "Now, let's merge the `feature/new-feature` branch into our main branch `master`. We'll see how the graphical representation changes."

[3:00 - 4:30]
**Visualizing the merged repository**
```bash
$ git log --all --oneline --graph
```
Narrator: "With the new commit, we can see that our branches are now connected again. This visual representation helps us understand how our commits are related."

[4:30 - 6:00]
**Visualizing an existing repository**
```bash
$ git clone https://github.com/tensorflow/tensorflow.git
```
Narrator: "Let's take a look at the TensorFlow repository, which is much larger and more complex. We'll use `git log` to visualize its branching structure."

[6:00 - 7:30]
**Ungitting**
```bash
$ git unstage
$ git unmerge
```
Narrator: "We'll use `git unstage` and `git unmerge` to undo our previous commits. This will give us a fresh start with the TensorFlow repository."

[7:30 - 9:00]
**Visualizing the final repository**
```bash
$ git log --all --oneline --graph
```
Narrator: "Finally, we'll visualize the final state of the repository using `git log`. This will show us the complex branching structure of the TensorFlow project."

[9:00 - 10:00]
**Conclusion**

Narrator: "In this video, we've explored how to use `git log` to visualize the branching structure of a repository. We started with a blank repository and created some commits, then merged them into our main branch. We also took a look at an existing repository, like TensorFlow, to see its complex branching structure."

[10:00]
**End of video**

Note: The timestamps are approximate and may vary depending on the specific system and environment being used.
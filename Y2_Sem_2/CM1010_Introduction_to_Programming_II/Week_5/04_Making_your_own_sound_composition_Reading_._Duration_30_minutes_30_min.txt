# Making your own sound composition
Reading•
. Duration: 30 minutes
30 min - Summary

URL: https://www.coursera.org/learn/uol-introduction-to-programming-2/supplement/DRu7H/making-your-own-sound-composition

This guide outlines the steps to complete a p5.js sketch focused on sound manipulation, following a demonstration by Simon. For initial resources, a template and video are provided, with the full code downloadable from the video resources or via the specific ZIP File link: 3.201 ZIP File. Working with the library requires consulting the detailed p5.js sound documentation for technical guidance. To complete the sketch, one must first create an `isInitalised` global variable, initially setting its value to `false`. This variable is crucial because it dictates when a message is drawn to the screen (when `false`) and when sound playback is allowed to start (upon the first key press).

Next, the user must explore the `loop` method's `rate` argument to understand its impact on sound playback dynamics. Furthermore, setting a specific `start time` is required for accurate sound timing; this can initially be set to zero. The functionality can be expanded by using the mouse X position in conjunction with the `map` function to dynamically change the playback rate when the sound is initiated. Another key action is to implement a mechanism that pauses the sound when the space bar is pressed. This necessitates adding a specific condition within the `keyPressed` function that checks for the space button press. To ensure the capability of restarting the sound playback smoothly, a nested conditional statement must be added, mirroring the structure shown in the instructional video.

A crucial technical step is to incorporate an anonymous function into the `loadSound` call. This function acts as a safeguard, effectively stopping any playback until the targeted audio file has been fully loaded into the sketch environment. Upon finishing the sketch, users are encouraged to explore other advanced methods provided by the comprehensive sound library. If different sound files are desired, free resources can be found by searching through a provided external link for available sounds. If any of the supplied links prove broken or non-functional, students are instructed to report the issue via the Student Portal. The primary focus remains on mastering these specific programmatic steps to build a fully interactive sound composition. The initial setup requires understanding both the video resources and the provided complete template code. Successfully implementing these steps allows for a deeper understanding of sound loading and dynamic control within p5.js. The objective is to synthesize multiple programming concepts—variables, event handling, mapping, and asynchronous loading—into a single, functional art piece.
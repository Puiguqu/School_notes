# Extract relevant data
Video•
. Duration: 9 minutes
9 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/wfygc/extract-relevant-data

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key >> In the last video, we used requests in Beautiful Stoop to load data from HTML pages and manipulate it using Python. In this video, we'll look at further processing that data and storing it in data frames. To do this, we'll first modify the procedure that we wrote to get data and parse it using BeautifulSoup. Then we'll use the data frame operation to convert it to pandas. In this process, we'll look at some additional procedures that we have to write in order to extract specific fields of data from HTML before converting it to pandas. To begin with, let's first look at the procedure that we wrote to get HTML data and modify it to return a collection of quotes. So the first few steps remain as before, where we get the requests get to fetch the response, check for a correct status of the response, parse that using Beautiful Soup, and also find all the div class quotes which contain the quotes in the HTML data. The only edit that we'll make is instead of printing the quotes, we'll return the quotes as a variable to the calling point. And let's store this in. Maybe call this in a variable called as soup quotes. Right. If we display soup quotes, we should see the different div elements containing the quotes, author names and respective tags as a list of values. So we see that that's the first. If so, our next step is to now convert this into a data frame. To do this, let's import pandas first. And the data frame operation can take a list of objects as its input and converts and creates that and creates a data frame based on that. The soup codes is a list of objects which we can pass. And let's see the resultant data frame that comes out. Not too bad. The data frame has done some conversion, although it's not the output that we quite expect. The output that we would ideally expect is three columns, one for the quotation, one for the author, and one maybe for a list of tags, or maybe six or seven columns, where one for the quote, one for the author, and then the remaining columns for the tags, each in a column. So let's modify our script to do something closer to that. To do this, we'll work on the soup codes variable and write an additional procedure that only extracts these specific fields that we want. Just before doing that, let's analyze just one element of the soup codes to know what we want to fetch and extract from the data. To get the quotes, we want to access the span element with the class of text. To get the author, we have to access the span element well, not really. Actually, the small element with a class called as author. And for each of the tags I will have to access the elements that have the class called as tag. Right. So let's extract these specific values, put them in an intermediate object, and then we'll create a data frame out of it. So first step will loop through every single object in the soup quotes list and perform this procedure for all those objects. Actually, let's, before that, create something like a list of objects list quotes. And we'll be keeping these objects stored in list quotes. So for each quote inquotes object, we'll do a find operation to find the span element by the class, the name of the class being the text. And this should be the text of the code. Let's quickly print to verify that that works. That works. Although I think we also have to add an additional text quote text that pulls the text from those. Right. To get the author name, let's do a find with the small class with click find a small element with class called as author. So that should get us the author tag. Again, let's just get the text of that, print the author names. Right, that looks correct. And lastly, we want to get a list of tags. So we're going to find. Now we're going to do find all because we have multiple tags and we're going to find the tags are stored in the HTML element called as a and the name of the class is called as tag. Let's just print the tags. We should find the list of values. Perfect. We should probably print the text for each of those. So if I just print the text, then I see the text. So what we could do is we could write an additional procedure that just pulls the text in tags. Let's create a list called as tags text and store only the tags from store only the text from each of these tags and then display tags underscore text. So now we have a list of values for each. Sorry, tax txt needs to be appended with all those values. I've just assigned those values, which is why I do not find a list of values. I only find a single value. So now I should find a list of values. This is for the first quote, for the second code, third code, fourth quote, and so on. All right, now that we have all these values, the remaining step is to write them all into a data frame. But before we do that, what we could do is what we need to do is create an object as data frames only, accept a list of objects to convert it to the correct format that we expect to see. So let's create an intermediate object, let's just call that coat object and create specific fields that we want to see. And these fields will be converted to the columns in the data frame. So the first one is the actual quote itself that we can assign as text code. Second one can be the author name, or let's just say author of the code which is stored, an author name, and finally a set of associated tags that we can add using the tag text. So now if we print each quote object, each of these objects is. Now each of these is an object that has three keys, one for quote, one for author, one for tags. Now the only remaining step is to plug this object into a data frame. Call this sorry, plug this object into a data frame. But actually we have to plug the list of quotes into the data frame. So plug this object into is to append this object with list of quotes and let's print the list of quotes. At the end of this, we should expect to see a list of objects which we can then pass on to our data frame. So now instead of seeing objects, we now see a list of objects. Each object has three values, a quotation, an author, a tag. And these objects can be passed into a data frame to create the data frame with those required columns. Right. As we expected, we now have a data frame with a quote, author and tags. If you would like to further split the tags, then one way to do that is to create tag number for each of these tags and store every element of the array as a separate tag. Then you would have one column each for each of the tags that are associated with a quote. But I leave that as an exercise for you to explore further. As a quick summary, we explored how to process data to be stored in data frames for further analysis. This required modifying an initial procedure that we wrote to get data from a HTML page. ing this, we wrote our own additional procedures to extract specific in the data, store them in an intermediate object and create a list of objects. On passing the list of objects, we were able to create a data frame with the required fields, quotations, authors and tags. That completes the procedure for extracting and storing data from HTML onto data frames. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web #### Lesson 11.1 Data organisation on the web - Introduction to retrieving data on the web Video . Duration: 2 minutes 2 min - Further reading: Handling data on the web Reading . Duration: 30 minutes 30 min - Introduction to data on the web Practice Assignment . Duration: 6 minutes 6 min - Organisation of HTML Video . Duration: 3 minutes 3 min - Organisation of HTML Practice Assignment . Duration: 9 minutes 9 min - HTML and XML Reading . Duration: 30 minutes 30 min - HTML and XML – model answer Reading . Duration: 10 minutes 10 min #### Lesson 11.2 Parsing HTML in Python - Introduction to webscraping Video . Duration: 11 minutes 11 min - Web scraping basics Lab . Duration: 1 hour 1h - Data sources on the internet Discussion Prompt . Duration: 30 minutes 30 min - Ethics of web scraping Practice Assignment . Duration: 6 minutes 6 min - Web scraping Practice Assignment . Duration: 15 minutes 15 min #### Lesson 11.3 Organise and analyse data - Extract relevant data Video . Duration: 9 minutes 9 min - Extract relevant data, data examination and analysis Practice Assignment . Duration: 20 minutes 20 min - Storing different types of data Lab . Duration: 1 hour 1h - Reviewing your programming assignment Reading . Duration: 10 minutes 10 min - Compare formats Reading . Duration: 10 minutes 10 min - Compare formats – model answer Reading . Duration: 10 minutes 10 min ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Extract relevant data ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
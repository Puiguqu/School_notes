# Data transformation
Video•
. Duration: 13 minutes
13 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/XZqWQ/data-transformation

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key So, let us look at techniques for preparing data for analysis. The main learning outcomes here are to load data from HTML that is stored in a CSV format, extract relevant fields from the data as well as analyze data formats and store data in the right format. To do this, we will look at data for transformation using the map function. So, let us first load pandas and numpy and read the file, read and examine the CSV file that is created from HTML. The, we are using the same practice dot CSV file that we that we loaded and stored in the previous topics. It has a name, price and stock, rating, review and a launch date column. Some steps in data preparation that we will adopt are extracting only the relevant names from well, extracting only the relevant fields from each of these columns. For instance, from name, we will remove product name and get only the name part such as hats, books, shoes, etcetera. From the price column, we will remove the tag price and then get only the price value as a preferably as a floating point. Similarly, for other fields such as in stock, rating, reviews, launch date, we will remove the tag that precedes the content and only extract the content. Additionally, we will look at, we will look at the content and examine whether they belong to the right data type. By that I mean that price needs to preferably be in the data type of float, rating preferably can be in the data type of integer, date in the data type of date and so on. So, we will also examine these data types and convert data to the right type wherever required. So, let us as a demonstration, let us look at three columns in this video, the name, price and rating. We will first extract the values, format the data types, check whether the data type has been corrected and stored in the right format and then we will proceed to the next column. Before we proceed to prepare data, in all these examples, we will use one specific function for data transformation and that is the map function. You can think of map function as one function that can apply to all the rows in a specific column in a data frame. As an example, I am creating the concatenate command that takes in a string, let us call that a title and returns, adds 10 to that title, adds the string 10 to the title and returns it. I can apply the map function by picking one of the columns of the data frame. Let us pick the name column, use the map function and pass the concatenate string. Using the concatenate function on every single, on every single row value of name. We see that 10 has been appended to every single value. So, using the map function is equivalent to applying a function, in this case concatenate on every single value of, on every single row value of a specific column. Using the same idea, we will write functions that can extract as well as format data types from those specific columns. So, let us look at the name column first. To get the name from, to remove the tag and extract only the name, we will split the string at the colon mark, remove all, remove all white spaces and return only the, and return only the hats, return only the item name part. We will first try this with an example of product name hats and apply the split operation. The split, remember that the split operation needs to exactly happen here. So, we find that split operation splits the string into two parts, a product name and a hats part. The hats part is the first element of the list. So, in order to access that, I can access the first element. I can access the hats through, by accessing the first element of the returned list. Note that hats also has an additional, hats also has an additional space at the starting. So, this can be removed by using the strip operation on a string that strips all the white spaces and returns only the content. So, in order to apply this, I have created a function called as extract title, which does the split, returns the first value, strips all the white spaces and returns the first value. Now, all that's remaining is to apply this onto the name function using the map operator. In the map space, instead of using concatenate, I will type an extract title. Let's just print and see what the results are. So, as we expected, the split operation has worked correctly. Note that by default, applying the map function does not make any changes to the data frame. So, in order to make changes to the data frame, we'll have to store the results of the map function back onto df.name. Now, let's print the data frame and we see that the changes are now reflected and, and all the tags prior to name have been removed. Let's proceed on and write a similar procedure for getting the, the price value. Now, let's see why this becomes a complication if we follow the same procedure. We have a price value. Let's say we split this at colon as before and return the first value. This works all right and sorry, strip the, strip the extra spaces. This returns all right when a value actually follows price, but we do, you can note that there are a few examples where there is no value that follows the price or this is empty. In these cases, strip only returns an empty value. Later on when this would be a problem, when we try to convert such empty values to floating points. So, we'll write a modified version of extract title that accounts for similar, that accounts for these errors while extracting the price. The first step is same as before. We split the string at the colon mark and let's call this just price. Let's also return the first value and strip the value. Now this can have two values depending on whether a number follows price or not. If no number follows price, then the value of price is an empty space. In which case we can return a special symbol called not number. This will be used to identify that the price column is a missing entry, is a missing data entry against specific rows. If price is not empty, we will return only the number, numeric part of the price, leaving aside the pound part. So in this case, then we will do an additional split on the price value, keeping pound as the delimiter and return the value to the right side of the pound, which is the first side. Now let's maybe give a couple of values and see if, see if our extract price mod function works as we intended to. So as a first example, let's give the first value of price. We've not returned anything, so let's just proceed to return. We find that 25.99 is returned as we expected. So first the extract price mod splits the string at the colon and then it checks the non-empty part and then splits it again at pound and then returns only the numeric part. Now let's give price as empty. We find that the special symbol not number has been returned. So this is all working as expected. So let's proceed on to apply this to the price column in the data frame using the map function. So let's store this in a temporary variable called as price. So on a closer examination, we find that price of zero is most likely, price of zero is a string. I can verify this further by finding the type of price and it is indeed a string. So one way to convert a string, we can convert the string to float by using the type conversion operation. So in the map function, we will essentially do the same where we use a map on the price list. Instead of passing any other function, we'll just directly pass in float and store it back to dfprice. Now I can print dfprice and verify that all the price values have now been converted to floating point values. The invalid values have been converted to a special symbol called as not a number. This is also correctly reflected in the source data frame. So let's now proceed on to ratings, which at the moment is a collection of stars. So we'll use the map function to convert a rating, which is displayed as a string of stars to a number. To do this conversion, we will use the text.count method. Text.count method can be applied onto any string. It takes in a string and a value to be searched and returns the number of occurrences of the search value. For instance, in this particular string, count returns three, whereas if I search for C, count returns zero. So in the stars procedure, we will simply return the rating value followed by count of the special symbol followed by the symbol star. So this should return the number of stars that are present in each of the rating values. To check this, let's pass one of the ratings as argument to the count stars function. And we find that this returns four, which is the correct answer as there are four stars in this particular rating. So let's apply this to all the elements in the rating column using the map function. So let's just pass count stars as the. And we find that count stars is counted all the stars from the top to the bottom. In the last bottom, there are no stars as a result, zero is returned as the value. So let's just store it back onto rating and verify that this has been reflected in the data frame. We can also verify that the new fields that have been created, namely price and rating are floats and integers respectively, and name is continues to be string as we want it to be. As a last step, we will write this data frame back to a CSV file. Let's call it practice dot practice corrected. And we can use this data frame for successive tasks such as cleaning the data. Note that I set the index value to false. The reason for that is if index is set to true, then when you reload the file back, then multiple indexes are created. And that's something that we would like to avoid. So while storing a data frame back to a CSV file, I would suggest putting the index value equals to false. As a final step, let's verify that the file indeed has been correctly saved. So we will read the same CSV file and display it on Jupyter notebook. We find that all the changes that we have made have persisted in practice underscore corrected dot CSV and we are able to successfully read it back. In the paired programming activity, I suggest you to load the practice dot CSV file, apply these data preparation steps and convert it to practice underscore corrected dot CSV file. There are also further steps in data preparation that you will need to explore. Namely, you will have to analyze in stock and convert it to a list of Boolean values. Finally, you will have to analyze launch date and convert launch date, which is currently in a string format to a pandas date and time format. On doing this, then you will be able to add additional kind of filters that allow you to examine the dataset using date as one of the possible indices. The instructions for transforming in stock review and launch date are available later on in the topic. I leave these as an exercise for you to follow the instructions and complete these tasks. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning #### Lesson 15.1 Introduction to data cleaning - Introduction to data cleaning Video . Duration: 1 minute 1 min - Data cleaning Reading . Duration: 2 hours 35 minutes 2h 35m - Introduction to data cleaning Practice Assignment . Duration: 6 minutes 6 min - Examining errors in data Video . Duration: 2 minutes 2 min - Examining errors in data Lab . Duration: 45 minutes 45 min #### Lesson 15.2 Data transformation - Data transformation Video . Duration: 13 minutes 13 min - Duplicates Practice Assignment . Duration: 3 minutes 3 min - Mapping and replacing values Practice Assignment . Duration: 6 minutes 6 min - Data transformation Lab . Duration: 1 hour 1h - Impact of errors Reading . Duration: 20 minutes 20 min - Impact of errors – model answers Reading . Duration: 10 minutes 10 min #### Lesson 15.3 Handling missing data - Identify common types of errors Practice Assignment . Duration: 9 minutes 9 min - Cleaning data Video . Duration: 12 minutes 12 min - Handling missing data Lab . Duration: 1 hour 1h - Legacy data Discussion Prompt . Duration: 20 minutes 20 min ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Data transformation ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
# Visualising text
Video•
. Duration: 13 minutes
13 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/KiuDe/visualising-text

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key >> So let's look at techniques for visualizing text data that occur in data sets. Visualizing text is not as straightforward as numbers as text needs to be processed and features need to be extracted before it can be visualized. In this video, we'll look at two techniques to extract features from text which can be used for visualization. The first of it is the countvectorizer. And in the second example we will look at extracting sentiments from text and using that to organize data for visualizing. Let's look at visualizing text feature through the countvectorizer. The countvectorizer is used in situations when you have text data that spans across multiple documents. If you remember from the previous lectures, we used frequency distribution to calculate the frequency of occurrence of words in a document. Countvectorizer is a generalization of that technique that applies to data that exists across multiple documents. So let's understand how countvectorizer works and how it can be used to derive features from text data that can be visualized. We start with a sample data set which contains an array of sentences. We can think of each of these sentences belonging to a different document. To use countvectorizer, we first initialize it and store it into a vectorizer into a variable. CountVectorizer is available as a method in SciPy feature extraction. SciPy's text feature extraction. It is a method and hence I have the parentheses following the vectorizer. In order to fit the vectorizer to the data, we use a FITT transform function and pass the documents as an argument. So let's call this word counts. We see that the vectorizer has returned a 4 into 9 sparse matrix. To actually to view the contents of the matrix, let's convert it into an array. So here we see the output of the vectorizer, which is a matrix that has four rows and nine columns. Each of the row corresponds to the document number, for instance the first, second, third and the fourth document. Each column refers to the collection of words that are present in all the documents. To better understand this, let me print the different features that the vectorizer has extracted. If we notice the vectorizer has extracted nine features that correspond to the collection of words that are present in all the documents. In the next step of our analysis, we will convert the output of the vectorizer into a data frame with which we can better visualize the results. The rows of the weight data frame. The values of the data frame are provided by the word count variable. The labels of the data set are provided by the feature by the output of the vector. Getfeature names. Let's store this in DF and print and review the output of df. My bad, it's the. So here it says that the shape of the values is 4 comma 1, whereas indices imply 4 comma 9. That's because I need to convert this to an array before I can plot it. Perfect. So now we see that the different column values are the name of the words. The different row numbers in the data frame are correspond to each of the documents. And the values of each of the cells in the data frame correspond to the frequency of occurrence of a specific word in the document. So in terms of plotting this, we need to get some aggregate statistics. One aggregate statistics that we can get from this data frame is the occurrences of a single word across all the documents. To find the word counts, we can simply use DF using column values as the index. By doing this, the number of occurrence of each word would be counted across the documents and the sum would be returned. So let's do this by using DF sum and inside let's give the access as column. Sorry, I may have misinterpreted it. The axis needs to be rows so that for each of these, for each column, the sum is computed across all the rows and we have and returned indicating the frequency of occurrence of a word in all the documents. Then as a last step, let's create two variables, one for words and one for. So let's call this DF1. Let's create two variables, one for the different words and one for the frequencies. And we will finally plot the frequencies against words. To get the frequencies, let's simply get the values and to get the words, let's just. Let's get the index values. Maybe this is just values and let's just convert this to an array and store it. Sorry, so this is importing NP. We'll store this in words. Similarly, let's print what DF1 values is and store it in frequencies. Let's plot the words and against their frequencies or let's plot the frequencies against the words as a bar plot to understand the the distribution of words across the different documents. Here we based on this we can find that the words document is the and this have been repeated four times and are the highest and are the most frequently occurring words in the data set. Words such as and one, second and third are the least occurring words which one occurrence each. The next technique that we will explore for text is to analyze the sentiment of text from reviews in a data set and plot the relative percentage or proportion of positive, negative and neutral sentiments. So there are different ways to analyze sentiments of text. There are different ways to analyze sentiments from text. One inbuilt function that NLTK provides is the the Vader sentiment analysis. This can be imported and the Vader sentiment analyzer takes in a line of text as input and computes a polarity score based on a pre trained model. By analyzing this polarity score, we are able to assign whether the review or whether the text has positive, negative or neutral sentiment. So let's look at a data set which contains a set of sentences that correspond to texts of different polarity. We will use a sentence sentiment analyzer. We will store this in a data frame, use a sentiment analyzer to analyze this text and assign sentiment labels. Finally, we will plot the distribution of sentiments in this data set as a pie chart. So the data that's available to us is stored in the variable called as data. So let's convert this into a data frame and plot this so the data set, the data frame has nine values with one column called as text. Using the sentiment analyzer, we will proceed to create another column called a sentiment. Quickly, let's understand how the sentiment analyzer works. We first initialize it using the sentiment analyzer method that we imported from nltk. Let's store this in sia. The method in SAA that computes the sentiment score is called the polarity scores. It takes in a sentence as input and outputs different scores corresponding to the polarity. In this case we are interested in, the output is the compound value which is the combination of the scores that the system has the model has assigned for negative, neutral and positive. If the compound score is less than zero, then it means that the statement has negative polarity. If it's equal to zero, then the statement is neutral and if it's positive, the statement has a positive. We will write our sentiment analyzer based on these particular rules and then apply it onto the data frame. So let's first define a function get sentiment that takes in text as input. The text is passed to the sentiment analyzer to return a value. If the compound value is greater than zero, then let's return positive sentiment else. If the value of the compound is less than zero, then we return negative sentiment. Otherwise we will return a neutral sentiment. Sorry, there is no else if it's an elif. And so now let's apply this to the data set that we have using the map function. So we will apply this onto the map function and store the values in a new column called as sentiment. So the text column is sent as input to the text column is used as input for the sentiment analyzer function. So in the map function we pass the sentiment get sentiment function as the argument which takes in each value of the text column and returns its sentiments stores it in DF sentiment. So what we wanted to do was since this is a dictionary, the syntax that would work is value followed by compound in brackets. Great. So let's now display df and we find that DF has an additional column which contains sentiment values for positive, negative and neutral. If we quickly check I love this product as a positive sentiment that looks alright, not great as negative and this is a product as a neural sentiment or it's a regular day is neutral. So it seems like the sentiments that have been assigned for the day, the data in the data set are quite consistent with what we would think are likely sentiments of those. So what remains to be done is to create a pie chart that demonstrates the distribution of the sentiment in the data set. To do this, we first have to find the number of positive, neutral and negative occurrences in the data set. We can do this by using the value counts function. The valuecounts function takes the name of the column as an input and returns a list of number of occurrences of positive, negative and neutral sentiments. So let's store this in a variable called as sentimentcounts. And as the last step we will use the code that I've already written to plot this and create a plot which shows the which shows that negative occurrences are most likely and common in the data set followed by positive followed by neutral which only contains two instances. The plot function contains an additional parameter called as startangle which can be passed in as a parameter to turn the data set by a specified angle for better presentation. Note that in addition to the title, I have commented the while label as an empty string. The reason for that is if this is commented or not specified, it appears as a y value which is not needed in this particular plot. So let me uncomment that and keep that as empty. And we have our pie chart for the different sentiments. In this video we looked at two ways of visualizing text data. We looked at deriving text data text features using the count vectorizers. We also looked at using nltkase Waiter to analyze sentiments in a data set and plot the relative percentage of positive, negative and neutral sentiments. In the paired programming activity. You will practice and apply these techniques. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing #### Lesson 18.1 Visualising text - Visualising text Video . Duration: 13 minutes 13 min - Exploratory data analysis techniques Reading . Duration: 30 minutes 30 min - Understanding metrics Practice Assignment . Duration: 9 minutes 9 min - Transforming text to numbers Practice Assignment . Duration: 12 minutes 12 min - Visualising text Lab . Duration: 1 hour 1h - Review of programming activity Reading . Duration: 5 minutes 5 min #### Lesson 18.2 Integrating metrics with visualisation - Metrics and visualisation Video . Duration: 1 minute 1 min - Metrics and visualisation Lab . Duration: 1 hour 1h - Metrics and visualisation Discussion Prompt . Duration: 30 minutes 30 min - Review your peer's work Practice Peer-graded Assignment . Duration: 1 hour 1h #### Lesson 18.3 Advanced programming activity - Advanced analysis Reading . Duration: 30 minutes 30 min - Text classification (optional activity) Lab . Duration: 1 hour 1h - Topic 9 summary Reading . Duration: 10 minutes 10 min ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Visualising text ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
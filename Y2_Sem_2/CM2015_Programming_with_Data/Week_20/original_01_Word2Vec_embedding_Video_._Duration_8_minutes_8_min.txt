# Word2Vec – embedding
Video•
. Duration: 8 minutes
8 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/qmIV6/word2vec-embedding

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key In the last few videos, we looked at different kinds of features, numeric, Boolean, and text-based features of data stored in data frames. We also looked at linear regression, a technique to find correlation between these features and make predictions. However, the linear regression technique that we discussed could be applied only to numeric data. Text-based data, in order to make predictions with text-based data, we need to additionally convert it to numeric formats and then use it for prediction or analysis using linear regression. Let's look at techniques to convert text to numeric formats and then use it for linear regression. To convert text to numbers, we will use a method called as Word2Vec. Word2Vec is a collection of models or architectures that takes in a word or a sentence as input and returns a vector of numbers as output. Each word is mapped on to a continuous vector space. Through an example, let's understand how Word2Vec can be applied on a dataset of words to create numbers. So let's start with a dataset stored in a data frame. Review text is an array of words and ratings is an array of integers. Let's store that in a data frame and visualize it. This is the data that we will be using to train Word2Vec as well as use it for predictions. Word2Vec itself is stored in a package called as Gensim. To import Word2Vec from Gensim, simply write from Gensim models import Word2Vec. Additionally, we will use the word tokenizer from NLTK and also the linear regression from sklearn from scikit-learn's linear model. So let's now look at using Word2Vec for getting words from each of the words in the dataset. The syntax for writing a Word2Vec is to provide a list of words as inputs at the time of initialization This goes into the variable called as sentences. So inside sentences, let's put each word that we get from the array, a list of words in dfreview.txt. Each word can be specified with a word vector size. Let's have a word vector size of 50. There are additional parameters such as the window, minimum count, and workers that I will leave at the default configuration. Now that we have the Word2Vec model, let's see if the model is initialized. Now that the Word2Vec model is initialized, let's look at how the word vector looks for one of the words in the model. To do that, you would do word2vec.wv followed by the name of the word that you want to find the vector for. So in this case, let's find the word2vec for the word poor. We see that this is an array that is of length 50 because we wanted the vector size to be 50. Each of these values are in the continuous space between, in this case, minus 1 to 1, for instance. Word2Vec additionally has a method that can be used to find the similarity or differences between a pair of words. The key idea behind Word2Vec models is that when words are embedded in a higher dimensional space, they are embedded in a way that the semantic relations are maintained. With Word2Vec models, we can expect that words like amazing and great would be closer in the vector space compared to words such as amazing and terrible or great and terrible. Let's use the Word2Vec similarity method to verify that this is the case. So I'm passing the two words amazing and terrible as input to the Word2Vec similarity method. Let me change one of these to make it as amazing. Let me change one of this and find the distance between amazing and great. Here I find that the distance between amazing and great is close to zero, indicating that Word2Vec considers these two words quite similar to each other or more similar to each other compared to amazing and terrible. So now that we understand how to use Word2Vec to get vectors from each of the words in our database, let's apply this to the dataset and use it for predictions. So as a first step, let's convert each of the words in the dataset to vector forms. To do this, we can use a map operation. Let's create a new column called as vectors that takes in review as the input, uses a map operation. The map operation will use a function that takes in review text as input and returns its vector as output. So let's create a little function that says Word2Vector that takes in a text as input. It uses the Word2Vec and returns the Word2Vec.w of text as output. So let's do Word2Vector here. Seems to be an error. It says that the word review is not correct. That's because the right name of the column is review text. So let's visualize the vector column of the Word2Vector. We see that each value of the vector column is now an array of values. Now we want to convert these arrays of values into features, namely X and Y. To do this, we will use the np stack to convert the values returned by df.vector column and store that in a variable called as Y. So this is the variable that we will use to train our model and also use for prediction. The Y values are simply the values of the ratings. Seems to be an error. That's because it's ratings without an S. So let's do this. So we have now our X and Y. What we have to do is create a linear regression model and we do that by calling the linear regression function and fit the X and Y values in the linear regression model. To get the predictions from linear regression, let's do linear regression dot predictions and we want to provide an input to make the predictions. So let's start with an input X. My bad, it probably is prediction. Okay, it's predict. So we see that regex predict takes in the vectors corresponding to each of the review texts in the data frame as input. The prediction value is a number that corresponds to rating of each of the texts that we fed as input. On comparing the values of the predictions and the values in the original list, we find that the regression model that we've developed is fairly accurate in making predictions based on the vectors. This is one approach to making predictions using text data. We first convert text to vector representations using the word2vec model. Then the different texts in the data set are converted into a feature vector X using the NPStack functionality. The ratings or any of the values that need to be predicted are stored in another variable called as Y. X and Y are then passed into linear regression to perform model training and predict the outputs. This sums up how to use text-based data for making predictions using linear regression. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction #### Lesson 20.1 Embeddings - Word2Vec – embedding Video . Duration: 8 minutes 8 min - Embeddings Practice Assignment . Duration: 6 minutes 6 min - Extracting embeddings Lab . Duration: 1 hour 1h - Word2Vec Reading . Duration: 30 minutes 30 min - Word2vec – model answer Reading . Duration: 10 minutes 10 min #### Lesson 20.2 Predict trends - Predict trends Video . Duration: 2 minutes 2 min - Predict trends Lab . Duration: 1 hour 1h - Analysing data Discussion Prompt . Duration: 15 minutes 15 min - Review your peer's work Practice Peer-graded Assignment . Duration: 1 hour 1h #### Lesson 20.3 Advanced programming activity - Optional activity: Enhancements Reading . Duration: 10 minutes 10 min - Enhancements (optional activity) Lab . Duration: 1 hour 1h - Topic 10 summary Reading . Duration: 10 minutes 10 min ### Week 21 Revision ### Week 22 Exam # Word2Vec – embedding ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
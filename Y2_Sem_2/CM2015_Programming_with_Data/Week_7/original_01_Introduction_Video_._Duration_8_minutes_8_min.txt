# Introduction
Video•
. Duration: 8 minutes
8 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/mnrPR/introduction

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key In the last few weeks, you've been learning how to create data and store them in data structures like lists and dictionaries. In typical situations, you will not need to create data, but it will be provided to you. Common forms of data storage include files such as text files, CSV files, or JSON files, or in more advanced cases, data stored on databases or retrieved from API calls. One key distinction between text and CSV and JSON is that text is an example of unstructured data, while CSV and JSON are examples of structured data. Let's start by understanding the facilities available in Python to load and read text files. For this example, we will use a welcome.txt file that contains two lines. We will write a program to read these lines as well as store them as lists and use these to create another text file. There are three modes of opening a file, a read mode, a write mode, and an append mode. We will look at the read mode first, which is just to read the contents of the file, and an encoding format. As you might be aware, characters, letters, symbols are stored as binary numbers in the computer. Encoding format is a specific mapping between a character or an alphabet and its binary equivalent. The common form of encoding is UTF-8, and we will use that to read the welcome.txt file. On successfully reading the file, the open operation returns a file handle. Typically, we store the file handle in a variable and use this variable to read or write the contents of the file. Printing the variable handle only prints the information about the handle. In order to actually read the contents of the file, we need to use two other methods. The first is the read operation, which reads all the contents of the file and returns it as a string. The second is a read line operation that reads one line of text from the file. Note here that I've used the read line operation after the read operation, and it has returned an empty space. The reason for this is once a read operation is completed, the file pointer reaches the end of the file. Any successive read operations hence returns an empty result. In order to perform a read line operation after a read operation, you would have to load the text file and then perform the read line operation. To examine the outputs of the read line operation, let's load the text file again and now let's do the read line operation. The first read line operation has returned the first line of the text file. In order to get the next line, use another read line operation and this returns the last line of the text file. Thus we can use successive read line operations to read a text file one line at a time. However, if you wanted to read all the lines of the text file, then you would use a variation of read line called as the read lines operation. This is a plural version of read line and has an S at the end. Oh, let me load the file first before I do the read lines operation. That I've loaded and I've done the read lines operation. The read lines operation returns all the text as a list of sentences. This is really convenient as I can use this list in a for loop and print every single line of the text file one line at a time. Let me first store this in a variable called as lines. Now lines should contain the different lines of the text file as a list. Looping through this in a for loop, we can print each and every line one line at a time. So far we've looked at the read mode for reading the content of the files. There are two other modes that are available to write information back into a file. These are the write mode and the append mode. The append mode is used to open a file and add more content to the end of the file. So let's open the welcome.txt file in the append mode and add a last line to this file. To append, to write content onto a file, we use the write command followed by the string that you would like to write onto the file. So let's write the string. This is the last line and open. Let's open the welcome.txt. We see that this has not yet been updated. The reason for that is once you write content onto a file, you have to close the file for the updates to happen. So let's close the file using the file handler and refresh the welcome.txt file. We see that the last line has been appended twice. That's because I ran the code twice, but we see that the last line has been appended at the end of the welcome.txt. The other mode to write content onto a file is the write mode. However, when you use the write mode, it overwrites the existing content of the file. So when you're opening, so always be careful when you're opening a file in the write mode as there is a risk of deleting content that already exists in the file. Let me demonstrate this through an example. We will open the welcome.txt file, but now in the write mode and write a single line of text back into that file. Remember that W is a string. The write mode is represented as a string. Let's add a single line called as all lines is deleted and close the file. Now when I open the welcome.txt file and refresh it, I see that in the write mode, all the lines are deleted and I'm no longer able to access the contents of the, um, all the lines are deleted. I'm no longer able to access the old content. However, write operation can be really useful when you want to create a new file and add content to it. So in this case, let's create a new file called as welcome2.txt in write mode and write a new string of values onto this file. So let's add a couple of lines. So we now see that welcome2.txt has been created and two lines have been added to it. Well, actually just one line and the reason for that is we do not have a delimiter. So let's open this file again and add a delimiter at the end of the first line and then write this back. Now we see that two lines have been added, a first line and a second line onto welcome2.txt. The last thing to understand is that it is possible to delete files from the Jupyter notebook interface. In order to do this, we will have to use the OS package available in Python. OS package provides a specific command called as remove, which takes in a name of a file and removes it from the folder. So in this case, I'm going to use the OS command to remove the welcome2.txt file. On refreshing, we find that welcome2.txt is no longer a part of the folder. However, if I try to remove welcome2.txt again from the folder, the OS remove presents an error as it is no longer able to find that file. So in general, it's good practice to first check if a file exists in a specific path before proceeding to delete it. To do this, use the command os.path.exist to check if a specific file is a part of a folder and then proceed on to remove that particular file. Similarly, print that the file is absent. In this case, we have already deleted welcome2.txt and as a result, the conditional logic says that the file is absent. In summary, we learned how to use the open command to read and write files in Python. We also learned the OS package and how it can be used to delete files. In the paired programming activity, you will explore how to use the open operation and count the number of characters that exist in a file. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system #### Lesson 7.1 Introduction to file handling - Introduction Video . Duration: 8 minutes 8 min - Exploring CSV format Video . Duration: 8 minutes 8 min - Further reading: Files and OS Reading . Duration: 10 minutes 10 min - Handling JSON files in Python Video . Duration: 7 minutes 7 min - Counting files the verbose way Lab . Duration: 1 hour 1h - Common problems while counting words from text files Reading . Duration: 5 minutes 5 min #### Lesson 7.2 Exploring different data formats - Further reading: References to python functions to read CS and JSON Reading . Duration: 10 minutes 10 min - CSV format – from spreadsheet to Python Practice Assignment . Duration: 6 minutes 6 min - File handling Practice Assignment . Duration: 9 minutes 9 min - Spreadsheet and JSON Practice Assignment . Duration: 6 minutes 6 min - CSV and JSON Lab . Duration: 1 hour 1h - Comparing CSV and JSON Practice Assignment . Duration: 20 minutes 20 min #### Lesson 7.3 Data frames - Data frames and exploratory analysis Video . Duration: 6 minutes 6 min - Data frames Practice Assignment . Duration: 9 minutes 9 min - Analysing and summarising outcomes Video . Duration: 7 minutes 7 min - Organisation and analysis Practice Assignment . Duration: 9 minutes 9 min - Practising data frames Lab . Duration: 1 hour 1h - Open data sources Discussion Prompt . Duration: 20 minutes 20 min ### Week 8 Topic 4 Reading and writing data Part 2 ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Introduction ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
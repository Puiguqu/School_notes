# Chatbot project part 3: Searching files
Video•
. Duration: 10 minutes
10 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/y5rUv/chatbot-project-part-3-searching-files

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Let's look at the third part of the assessments of the mid-term assessments involving the chatbot. Over the last few weeks, we've been looking at creating precepts and responses for the chatbot by hand coding them as variables in the Jupyter notebook. From this week onwards, we'll build the chatbot by loading the precepts and responses from a JSON file. In terms of the chatbot architecture, we are moving on to a slightly more advanced architecture where the chatbot associates each pattern with an intent and converts that intent to a response. The JSON file contains a list of objects which have three components, one, a pattern, two, a tag associated with the pattern or an intent associated with the pattern, and three, a response associated with the pattern and the intent. In terms of the chatbot flow, you will first read the JSON file to store tags, patterns and responses. Then you will examine whether some of these patterns are regular expressions. If some of these patterns are regular expressions, then it might be best to convert them to one format, preferably the regex format. Then using the combination of patterns, tags and intents, we'll create two dictionaries, one called regex2int and the other called intoresponse. The regex2int dictionary stores the associations between patterns, that patterns observed in the user input and possible intents that can be associated with them. The tags2, the intent to response dictionary contains a mapping from intents that have been identified in a user input to responses, which can be generated by the chatbot. Finally, you will integrate this with the rest of the chatbot code. So in terms of demonstration, let's step through each of these tasks and understand what they are about. For the very first task, open the intents.json file and look at its structure. We are going to look at this file and use pandas to read this file onto Jupyter notebooks. The intents.json contains a high level object called as intents, which contains a list of objects. Each of these objects in the list contains three components, a tag, which is called greeting or goodbye, patterns, which are associated with that particular tag and responses which are associated with the patterns and tags. So as a first step, we will load this onto Jupyter notebook and get these as independent lists. From then on, you can zip these different lists onto a dictionary and use the dictionary for chatbot pattern recognition and response generation. As a quick note, completing this exercise requires an understanding of using pandas to load and manipulate JSON files. These are the videos pertaining to this part of the material are available earlier in the topic. So, I would suggest getting familiar with the material on using pandas to manipulate JSON files before you move on to complete this particular task. So, as the first step, load the JSON file using pandas and examine its contents. So, intents shows that there is a top level intent, there is a top level intent and each of these intent is an object. So for instance, if I wanted to access the tag of the greeting, then I would access the first element of the intents and then get the greeting. In the remaining steps, use a for loop to loop through all the intents and create three different lists that correspond one to tags, one to responses and another two patterns. Some of these patterns might be regular expressions and some of these patterns might be string. Here is a handy function that you could use to convert strings to regular expressions and this involves using a handy operation that converts strings to regular expressions. Let us say you have a list of strings. This is similar to what you might have in an array containing the tags, an array containing the patterns. So, let us call this patterns which contains a list of values. Your task would then be to string all these together into a regular expression that looks like this. While it is possible to do this when the list contains a small number of elements, it might be better to come up with an automated procedure. The automated procedure essentially involves using a pipe operation followed by a join operation on the list. This contains a regular expression which takes each of the elements of the patterns list and joins them via a pipe symbol. This would be really useful when you are parsing through a patterns list to create a regular expression. Let us move on to the next step in the process. Once you have loaded patterns, tags and responses in their respective lists, we will create two dictionaries based on this. So, write a function name called as store patterns. This takes tags in patterns as input and converts them into a regular expression. Now that we have stored patterns, tags and responses in a list, let us continue to create two dictionaries. The first dictionary is called regex2int which uses the regex patterns as keys and the tags as intents. The second dictionary is int2res which uses intents as keys and responses as values. To create regex2int, let us first write a function name called as store patterns. In this pattern, use a for loop to loop through every single pattern in the patterns list. For every pattern, pick the associated tag and append it onto a dictionary called regex2int. The expected output of regex2int based on the intents file is given in the example. There seems to be more categories than I had originally intended. In your case, what I would suggest is add categories similar to this in your intent on JSON file. But to keep things consistent, this is the output format of regex2int based on the intents file that you currently have. The patterns, hi, how are you, hello, good day, the key part of the regex2int, greeting is the associated value. Similarly, crcu before bycu later are the key part of the regex2int and goodbye is the value. Write a similar procedure to use the responses and tags lists to create a dictionary. In this case, this is different from the earlier dictionary in that the value of the tags is used as keys and the responses are used as values. So based on the intent.json file that we currently have, for each tag, there are a bunch of responses. The tag greeting has hello, good to see you again, hi there, how can I help, along with other responses. Similarly, goodbye has a few responses such as see to go talk to you later and so on. As you expand the intents.json file to create the chatbot specific to your domain, I would suggest adding more tags and responses such as ours number. But for this particular example, based on the intents.json file, the correct output is greetings and goodbye. The last step is to integrate the regex2int and intoresponse in the chatbot code. Let's create three cells, one for loading the files, the second for pattern matching and the third for the main chatbot loop. So if I paste the main chatbot loop from previous iterations, some of the things that we would have to change is remove the logic for pattern searching from the main loop and replace it by a function called as response. Create a new function called as generateResponse that takes in a user input, checks for the user input in regex2int and then uses the tag in, tag of regex2int in intoresponse to get the output. Finally, if there is a match, return the match or return, if there is a match, print the match or return false in order to print the default chatbot output. Finally, write the, write a separate function to load the JSON files. Once you finish load files, call this in generateResponse function to create, to get the two dictionaries regex2int and intoresponse. Then the overall flow of the chatbot is that in the main loop, generateResponses is called which in turn calls load files to get the responses from intents.json file. In particular, it gets data from the intent.json file in as two dictionaries regex2int and intoresponse. The user's input is matched against regex2int using a regular expression search. Match the patterns between user input and regex2int to find a corresponding intent. The intent is passed into intoresponse to get the output. If there is a matching output, then return the output or else return false. If there is a match, print the match and return true, otherwise return a false. If no match is found, then the chatbot generates the default response. From this point onwards, I would suggest creating two files for the chatbot. One is the file that is stored in the Jupyter notebook which contains the code that runs the chatbot. Second is the data that the chatbot uses such as the tags, responses and patterns. These are stored in the intents.json file. Keep these two files separate so that edits in one file are modular and do not affect the operation of the other file. Keep expanding and in the next video on chatbot, we will look at more text preprocessing techniques. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 #### Lesson 8.1 Structured representations of text - Searching structured representations Video . Duration: 3 minutes 3 min - Structured representations of text Practice Assignment . Duration: 9 minutes 9 min - Searching structured representations Lab . Duration: 1 hour 1h #### Lesson 8.2 Chatbot project Part 3 - Chatbot project part 3: Searching files Video . Duration: 10 minutes 10 min - Chatbot Part 3 Lab . Duration: 1 hour 1h - Chatbot project part 3 Discussion Prompt . Duration: 30 minutes 30 min - Chatbot part 3 Practice Peer-graded Assignment . Duration: 30 minutes 30 min #### Lesson 8.3 File handling in the OS - Handling files in the OS Video . Duration: 13 minutes 13 min - Navigating OS with Python Practice Assignment . Duration: 9 minutes 9 min - Handling errors in paths Practice Assignment . Duration: 12 minutes 12 min - Loading and navigating (optional activity) Lab . Duration: 1 hour 1h - Topic 4 summary Reading . Duration: 10 minutes 10 min ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Chatbot project part 3: Searching files ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.
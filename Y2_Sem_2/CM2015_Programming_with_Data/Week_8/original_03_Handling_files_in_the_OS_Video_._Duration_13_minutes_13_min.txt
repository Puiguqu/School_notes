# Handling files in the OS
Video•
. Duration: 13 minutes
13 min

URL: https://www.coursera.org/learn/uol-cm2015-programming-with-data/lecture/MO4Cu/handling-files-in-the-os

## VIDEO TRANSCRIPT ## You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key So, through this topic, we've been looking at storing data and files in reading and manipulating them through Python programs. Typically, in most computers, file management is handled by the operating system. The OS module provides an interface between the Python interpreter and the operating system. In this video, we will examine the different commands available in the OS module through a practical example of creating a directory structure for your chatbot project. One way to organize the chatbot project is to have a top-level directory with several subdirectories. Each of these subdirectories contain different files associated with that week's iteration of the chatbot, such as the code files and the readme files. Now, we will use the OS module to create a directory structure that's similar to the one that you observed, as well as write procedures to search and filter specific files from that directory. Let's look at the code for this. To use the different methods of the OS module in Python, you first have to import it. Before I use the OS module to manipulate files and directories, I first check the current directory that I'm working in. To do this, you would use the git cwd command in the OS module. This gives the name of the current directory that I'm working in. The output corresponds to the top-level directory which contains all the files associated with this particular topic. In order to create a new directory, use the make directory command followed by the path and the name of the folder or the directory that you want to create. In this case, I'm going to create a directory called as chatbot-test. In successfully executing the make directory command, I'm able to create another top-level directory called as chatbot-test. Now there are two ways to create subdirectories for each of the parts of the chatbot. One way to do that is, again, to use the make directory command, but specify the complete path to the subdirectory. For instance, if I wanted to create a subdirectory called as part1, then I would give the complete path which is chatbot-test followed by the name of the subdirectory, part1. Now let's verify that part1 has indeed been created inside chatbot-test. Another way to do that is to change the directory that you're currently working in. To do this, you would use the change directory command or chdir command. Using this command, I can change my current working directory as the chatbot-test. How do I know that the change has happened? I would use the git cwd command, and now I see that the new directory that I'm currently working in is chatbot-test. Within this directory, I can directly use the make dir command and create more subdirectories such as part2 and part3. Let's go back and observe that the different directories have been created as we intended. Let's say, for instance, you have an additional directory that is not a part of your portfolio that you would like to remove. Let's call this part4. In order to remove this, you would use the rmdir command. Again, the rmdir command takes the path to the directory as well as the name of the directory. In this case, we are currently in the chatbot-test directory, so I'm going to simply type part4. Oh, I seem to have made a mistake and left a space between part and 4. Now I should expect to see the part4 directory disappear from my list, which has happened. One thing to note about the rmdir command is that it does not allow you to remove directories that have existing files inside them. The reason for this is that you wouldn't want to accidentally delete directories that already have files. So in order to check this, let's actually go back to the change working directory to the top-level directory. The way you would do that is using the chdir command. The top-level directory is one level above, so in order to move one level above, you would write a double dot followed by a slash. If you wanted to move two levels above, then you would do a double dot followed by a slash and another double dot followed by a slash. But in this case, we just want to move one level to the top-level working directory, so we will do two dots followed by a slash. Let's verify that we are indeed at the top-level directory. Now we are going to use the remove directory command to remove a directory that has some files in it. For instance, let's try to remove the chatbot directory. Now the rmdir command indicates an error, which is that the directory is not empty and it would not allow you to remove it. Now we will write a procedure to do that in chatbot underscore test. So as a first step, we will have to list all the subdirectories that are inside chatbot underscore test. To do this, we will use the list underscore directory command. The list underscore directory command takes a directory as input and lists all the files and subdirectories inside it. In this case, we only contain three subdirectories and all these subdirectories are empty, so we can proceed to remove these subdirectories. To remove the subdirectories, we will essentially use a for loop and iterate through all the outputs that are produced by list underscore directory, which is part one, part two, and part three, and use the os remove directory command for each of these. But before we do that, let's just verify that we are getting the exact directory names that we want. Now we will proceed to use the remove directory command and remove all these subdirectories. So I'm already seeing an error, which is that part one is not present in chatbot underscore test. A very likely reason for this is that it's probably already removed. Ah, no, the reason for this is that I'm not giving the right path to part one of chatbot underscore test. So again, I can do this in a couple of ways. One is I can first change my working directory to chatbot underscore test and then try this. It still says no because now I can't use chatbot underscore test here. So let me do this in another way, which is give the complete path to every single file under chatbot underscore test. The way I would do that is to say chatbot underscore test, followed by a slash, followed by the name of the file. Let's just print and verify that this indeed gives us the entire path of the file of the directory that we want to remove. Pardon, I seem to have made a couple of mistakes here. Now this is perfect. Now we're going to print each subdirectory in chatbot underscore test and print the complete path to that. The complete path to that subdirectory is a chatbot underscore test, followed by part one, part two, and part three. So now we've been able to construct the full file path for each of these. We're going to proceed by using the rmdir command and remove these. So now let's go back and verify that we have successfully removed all the other, all the subdirectories inside chatbot underscore test. Now we can use the rmdir command on chatbot underscore test, and this will proceed to remove the directory that we created. The next thing we want to understand is how can we search for a specific file inside the directory? So one way to do that is to walk through all the files, folders, and subfolders inside a specific directory. The command that lets you do that is the os.walk command. What the os.walk command does is it takes a name of a directory and then returns all the folders, subfolders, as well as the files inside the directory. It returns it as a generator, so you would have to put this in a for loop and then print the output one by one. So let's do an os.walk on the chatbot folder and print all the files and folders. The os.walk returns three outputs. One is the root level directory, the subdirectory, and the files. So let's store those in, let's just call this subdirectory and files. Let's just store these in three different variables and print their values. When the root directory is chatbot, the subdirectories are part one and part two, and there are no other files in that particular root directory, so we see no files. When the root directory is part one, we see that the subdirectory contains one folder, which is Python checkpoints, and the different files are readme.txt and code.ipynb. When the root directory is part two, we see that there are no other subdirectories or files associated. The os.walk produces a generator object, and hence to review its outputs, we have to put it inside a for loop. Specifically, the os.walk gives a tuple of outputs that contain three values, namely the root level directory, the name of the subdirectory, and the files associated in the directory. So let's do an os.walk on chatbot and quickly review its outputs by printing the root level directory, subdirectory, and the files. So the way that we would interpret this output is when the root level directory is chatbot, the different subdirectories are part one, Python checkpoints, and part two, and there are no other files in this root directory. When the root directory is part one, the subdirectory are the Python checkpoints, and the different files are the readme and the code files. Now if you want to do a search operation and retrieve files that have specific types, such as text files or a Jupyter notebook, we will use the files that are returned by the os.walk command and check if the files have specific extensions. So let's comment this out and only work with the file output of the os.walk command. Specifically, we are now looking at whether, now let's loop through each of the files that are returned and check if the files end with a specific extension, such as a Jupyter notebook file. To do that, you would use the endswith command and check if it ends with ipynb, and if the file ends with ipynb, then we'll print the name of the file. Let's run this and see the outputs. So here what's happening is that for each of the files in each of the directories and subdirectories inside the chatbot, we are retrieving those files as a list using another for loop to loop through every single file in that list. And it checks if that the name of the file or the file string ends with, so these are the files that are actually tested. We are checking if the file string ends with an ipynb and then printing if there is a match. If we would like to change this to printing only the text files, then we would check if the file ends with a .txt and then print only those specific files from the directories. In summary, we learned about the OS command and how it can be used as an interface between the Python interpreter and the operating system in your computer. OS contains specific methods to create directories and subdirectories as well as remove them. At the end, we looked at the list directory command and how it can be used to enumerate all the files and subdirectories inside a directory. The list directory command can be combined with for loops to perform operations such as removing directories as well as filtering directories and retrieving a specific file format. Now, let us practice this in the parrot programming activity. ## END TRANSCRIPT ## ## ADDITIONAL PAGE CONTENT ## ### Week 1 Topic 1 Introduction to the course ### Week 2 Topic 1 Getting to grips with Python ### Week 3 Topic 2 Python basics: Data types, control flow, and functions ### Week 4 Topic 2 Writing programs using lists and control flow ### Week 5 Topic 3 Data structures ### Week 6 Topic 3 Pattern matching with strings ### Week 7 Topic 4 Reading and writing data on the file system ### Week 8 Topic 4 Reading and writing data Part 2 #### Lesson 8.1 Structured representations of text - Searching structured representations Video . Duration: 3 minutes 3 min - Structured representations of text Practice Assignment . Duration: 9 minutes 9 min - Searching structured representations Lab . Duration: 1 hour 1h #### Lesson 8.2 Chatbot project Part 3 - Chatbot project part 3: Searching files Video . Duration: 10 minutes 10 min - Chatbot Part 3 Lab . Duration: 1 hour 1h - Chatbot project part 3 Discussion Prompt . Duration: 30 minutes 30 min - Chatbot part 3 Practice Peer-graded Assignment . Duration: 30 minutes 30 min #### Lesson 8.3 File handling in the OS - Handling files in the OS Video . Duration: 13 minutes 13 min - Navigating OS with Python Practice Assignment . Duration: 9 minutes 9 min - Handling errors in paths Practice Assignment . Duration: 12 minutes 12 min - Loading and navigating (optional activity) Lab . Duration: 1 hour 1h - Topic 4 summary Reading . Duration: 10 minutes 10 min ### Week 9 Topic 5 Processing text data ### Week 10 Topic 5 Language processing ### Week 11 Topic 6 Retrieving data from the web ### Week 12 Topic 6 Retrieving data from the web ### Week 13 Topic 7 Retrieving data from the databases ### Week 14 Retrieving data from the databases ### Week 15 Data cleaning ### Week 16 More data transformations ### Week 17 Data analysis and visualisation ### Week 18 Exploratory analysis for language processing ### Week 19 Advanced topics in Python and data programming ### Week 20 Advanced feature extraction ### Week 21 Revision ### Week 22 Exam # Handling files in the OS ## Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press CTRL + S. To expand your selection you may use CTRL + arrow key. You may contract your selection using shift + CTRL + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using CTRL in conjunction with the alt key Theater mode available Enjoy an immersive video experience with dark mode and an auto-scrolling transcript.